// Null e Undefined

// Null é utilizado para indicar intensionalmente que não existe valor atribuido a uma variável
let nullVariable = null
console.log(nullVariable)

// Undefined é o valor que o JavaScript atribui automaticamente a uma variável que não armazena nenhum valor
let undefinedVariable
console.log(undefinedVariable)
